# TODO:
# Read a data file with pyspark
# Make some operations on each column, in order to recognize the type of the column
# Example: if the number of distincts are much less than the size of the columns it's discrete
# GET DISTINCTS

# if the type of the column is numerical, get]
# GET MAX and MIN DATES 
# if the type of column is date and it's ordered, get the 